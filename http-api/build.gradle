plugins {
    id 'java'
    id 'groovy'
    id 'application'
}

mainClassName = 'com.zopa.greeter.Greeter'

dependencies {
    compile project(':component-test') 
    testCompile 'org.spockframework:spock-core:1.0-groovy-2.4', {
        exclude module: 'groovy-all'
    }
}

